#include <stdio.h>
#include <string.h>

#define WORDS "beast"
#define SIZE 40

int main(int argc, const char *argv[])
{
    const char *orig = WORDS;
    char copy[SIZE] = "Be the best that you can be.";
    char *ps;

    puts(orig);
    puts(copy);
    
    return 0;
}
